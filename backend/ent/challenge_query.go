// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ZacharyZcR/STC/backend/ent/challenge"
	"github.com/ZacharyZcR/STC/backend/ent/challengehint"
	"github.com/ZacharyZcR/STC/backend/ent/challengeinstance"
	"github.com/ZacharyZcR/STC/backend/ent/challengetag"
	"github.com/ZacharyZcR/STC/backend/ent/checkresult"
	"github.com/ZacharyZcR/STC/backend/ent/competitionchallenge"
	"github.com/ZacharyZcR/STC/backend/ent/flagsubmission"
	"github.com/ZacharyZcR/STC/backend/ent/predicate"
	"github.com/ZacharyZcR/STC/backend/ent/scorerecord"
	"github.com/ZacharyZcR/STC/backend/ent/user"
	"github.com/ZacharyZcR/STC/backend/ent/writeup"
)

// ChallengeQuery is the builder for querying Challenge entities.
type ChallengeQuery struct {
	config
	ctx                       *QueryContext
	order                     []challenge.OrderOption
	inters                    []Interceptor
	predicates                []predicate.Challenge
	withAuthor                *UserQuery
	withHints                 *ChallengeHintQuery
	withTags                  *ChallengeTagQuery
	withCompetitionChallenges *CompetitionChallengeQuery
	withFlagSubmissions       *FlagSubmissionQuery
	withChallengeInstances    *ChallengeInstanceQuery
	withScoreRecords          *ScoreRecordQuery
	withCheckResults          *CheckResultQuery
	withWriteups              *WriteupQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChallengeQuery builder.
func (_q *ChallengeQuery) Where(ps ...predicate.Challenge) *ChallengeQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ChallengeQuery) Limit(limit int) *ChallengeQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ChallengeQuery) Offset(offset int) *ChallengeQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ChallengeQuery) Unique(unique bool) *ChallengeQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ChallengeQuery) Order(o ...challenge.OrderOption) *ChallengeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAuthor chains the current query on the "author" edge.
func (_q *ChallengeQuery) QueryAuthor() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, challenge.AuthorTable, challenge.AuthorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHints chains the current query on the "hints" edge.
func (_q *ChallengeQuery) QueryHints() *ChallengeHintQuery {
	query := (&ChallengeHintClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(challengehint.Table, challengehint.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.HintsTable, challenge.HintsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTags chains the current query on the "tags" edge.
func (_q *ChallengeQuery) QueryTags() *ChallengeTagQuery {
	query := (&ChallengeTagClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(challengetag.Table, challengetag.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.TagsTable, challenge.TagsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCompetitionChallenges chains the current query on the "competition_challenges" edge.
func (_q *ChallengeQuery) QueryCompetitionChallenges() *CompetitionChallengeQuery {
	query := (&CompetitionChallengeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(competitionchallenge.Table, competitionchallenge.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.CompetitionChallengesTable, challenge.CompetitionChallengesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFlagSubmissions chains the current query on the "flag_submissions" edge.
func (_q *ChallengeQuery) QueryFlagSubmissions() *FlagSubmissionQuery {
	query := (&FlagSubmissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(flagsubmission.Table, flagsubmission.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.FlagSubmissionsTable, challenge.FlagSubmissionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChallengeInstances chains the current query on the "challenge_instances" edge.
func (_q *ChallengeQuery) QueryChallengeInstances() *ChallengeInstanceQuery {
	query := (&ChallengeInstanceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(challengeinstance.Table, challengeinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.ChallengeInstancesTable, challenge.ChallengeInstancesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryScoreRecords chains the current query on the "score_records" edge.
func (_q *ChallengeQuery) QueryScoreRecords() *ScoreRecordQuery {
	query := (&ScoreRecordClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(scorerecord.Table, scorerecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.ScoreRecordsTable, challenge.ScoreRecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCheckResults chains the current query on the "check_results" edge.
func (_q *ChallengeQuery) QueryCheckResults() *CheckResultQuery {
	query := (&CheckResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(checkresult.Table, checkresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.CheckResultsTable, challenge.CheckResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWriteups chains the current query on the "writeups" edge.
func (_q *ChallengeQuery) QueryWriteups() *WriteupQuery {
	query := (&WriteupClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(challenge.Table, challenge.FieldID, selector),
			sqlgraph.To(writeup.Table, writeup.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, challenge.WriteupsTable, challenge.WriteupsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Challenge entity from the query.
// Returns a *NotFoundError when no Challenge was found.
func (_q *ChallengeQuery) First(ctx context.Context) (*Challenge, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{challenge.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ChallengeQuery) FirstX(ctx context.Context) *Challenge {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Challenge ID from the query.
// Returns a *NotFoundError when no Challenge ID was found.
func (_q *ChallengeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{challenge.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ChallengeQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Challenge entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Challenge entity is found.
// Returns a *NotFoundError when no Challenge entities are found.
func (_q *ChallengeQuery) Only(ctx context.Context) (*Challenge, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{challenge.Label}
	default:
		return nil, &NotSingularError{challenge.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ChallengeQuery) OnlyX(ctx context.Context) *Challenge {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Challenge ID in the query.
// Returns a *NotSingularError when more than one Challenge ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ChallengeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{challenge.Label}
	default:
		err = &NotSingularError{challenge.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ChallengeQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Challenges.
func (_q *ChallengeQuery) All(ctx context.Context) ([]*Challenge, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Challenge, *ChallengeQuery]()
	return withInterceptors[[]*Challenge](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ChallengeQuery) AllX(ctx context.Context) []*Challenge {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Challenge IDs.
func (_q *ChallengeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(challenge.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ChallengeQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ChallengeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ChallengeQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ChallengeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ChallengeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ChallengeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChallengeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ChallengeQuery) Clone() *ChallengeQuery {
	if _q == nil {
		return nil
	}
	return &ChallengeQuery{
		config:                    _q.config,
		ctx:                       _q.ctx.Clone(),
		order:                     append([]challenge.OrderOption{}, _q.order...),
		inters:                    append([]Interceptor{}, _q.inters...),
		predicates:                append([]predicate.Challenge{}, _q.predicates...),
		withAuthor:                _q.withAuthor.Clone(),
		withHints:                 _q.withHints.Clone(),
		withTags:                  _q.withTags.Clone(),
		withCompetitionChallenges: _q.withCompetitionChallenges.Clone(),
		withFlagSubmissions:       _q.withFlagSubmissions.Clone(),
		withChallengeInstances:    _q.withChallengeInstances.Clone(),
		withScoreRecords:          _q.withScoreRecords.Clone(),
		withCheckResults:          _q.withCheckResults.Clone(),
		withWriteups:              _q.withWriteups.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAuthor tells the query-builder to eager-load the nodes that are connected to
// the "author" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithAuthor(opts ...func(*UserQuery)) *ChallengeQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAuthor = query
	return _q
}

// WithHints tells the query-builder to eager-load the nodes that are connected to
// the "hints" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithHints(opts ...func(*ChallengeHintQuery)) *ChallengeQuery {
	query := (&ChallengeHintClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHints = query
	return _q
}

// WithTags tells the query-builder to eager-load the nodes that are connected to
// the "tags" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithTags(opts ...func(*ChallengeTagQuery)) *ChallengeQuery {
	query := (&ChallengeTagClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTags = query
	return _q
}

// WithCompetitionChallenges tells the query-builder to eager-load the nodes that are connected to
// the "competition_challenges" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithCompetitionChallenges(opts ...func(*CompetitionChallengeQuery)) *ChallengeQuery {
	query := (&CompetitionChallengeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCompetitionChallenges = query
	return _q
}

// WithFlagSubmissions tells the query-builder to eager-load the nodes that are connected to
// the "flag_submissions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithFlagSubmissions(opts ...func(*FlagSubmissionQuery)) *ChallengeQuery {
	query := (&FlagSubmissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFlagSubmissions = query
	return _q
}

// WithChallengeInstances tells the query-builder to eager-load the nodes that are connected to
// the "challenge_instances" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithChallengeInstances(opts ...func(*ChallengeInstanceQuery)) *ChallengeQuery {
	query := (&ChallengeInstanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChallengeInstances = query
	return _q
}

// WithScoreRecords tells the query-builder to eager-load the nodes that are connected to
// the "score_records" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithScoreRecords(opts ...func(*ScoreRecordQuery)) *ChallengeQuery {
	query := (&ScoreRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withScoreRecords = query
	return _q
}

// WithCheckResults tells the query-builder to eager-load the nodes that are connected to
// the "check_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithCheckResults(opts ...func(*CheckResultQuery)) *ChallengeQuery {
	query := (&CheckResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCheckResults = query
	return _q
}

// WithWriteups tells the query-builder to eager-load the nodes that are connected to
// the "writeups" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChallengeQuery) WithWriteups(opts ...func(*WriteupQuery)) *ChallengeQuery {
	query := (&WriteupClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWriteups = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Challenge.Query().
//		GroupBy(challenge.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ChallengeQuery) GroupBy(field string, fields ...string) *ChallengeGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ChallengeGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = challenge.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.Challenge.Query().
//		Select(challenge.FieldTitle).
//		Scan(ctx, &v)
func (_q *ChallengeQuery) Select(fields ...string) *ChallengeSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ChallengeSelect{ChallengeQuery: _q}
	sbuild.label = challenge.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ChallengeSelect configured with the given aggregations.
func (_q *ChallengeQuery) Aggregate(fns ...AggregateFunc) *ChallengeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ChallengeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !challenge.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ChallengeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Challenge, error) {
	var (
		nodes       = []*Challenge{}
		_spec       = _q.querySpec()
		loadedTypes = [9]bool{
			_q.withAuthor != nil,
			_q.withHints != nil,
			_q.withTags != nil,
			_q.withCompetitionChallenges != nil,
			_q.withFlagSubmissions != nil,
			_q.withChallengeInstances != nil,
			_q.withScoreRecords != nil,
			_q.withCheckResults != nil,
			_q.withWriteups != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Challenge).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Challenge{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAuthor; query != nil {
		if err := _q.loadAuthor(ctx, query, nodes, nil,
			func(n *Challenge, e *User) { n.Edges.Author = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withHints; query != nil {
		if err := _q.loadHints(ctx, query, nodes,
			func(n *Challenge) { n.Edges.Hints = []*ChallengeHint{} },
			func(n *Challenge, e *ChallengeHint) { n.Edges.Hints = append(n.Edges.Hints, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTags; query != nil {
		if err := _q.loadTags(ctx, query, nodes,
			func(n *Challenge) { n.Edges.Tags = []*ChallengeTag{} },
			func(n *Challenge, e *ChallengeTag) { n.Edges.Tags = append(n.Edges.Tags, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCompetitionChallenges; query != nil {
		if err := _q.loadCompetitionChallenges(ctx, query, nodes,
			func(n *Challenge) { n.Edges.CompetitionChallenges = []*CompetitionChallenge{} },
			func(n *Challenge, e *CompetitionChallenge) {
				n.Edges.CompetitionChallenges = append(n.Edges.CompetitionChallenges, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withFlagSubmissions; query != nil {
		if err := _q.loadFlagSubmissions(ctx, query, nodes,
			func(n *Challenge) { n.Edges.FlagSubmissions = []*FlagSubmission{} },
			func(n *Challenge, e *FlagSubmission) { n.Edges.FlagSubmissions = append(n.Edges.FlagSubmissions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChallengeInstances; query != nil {
		if err := _q.loadChallengeInstances(ctx, query, nodes,
			func(n *Challenge) { n.Edges.ChallengeInstances = []*ChallengeInstance{} },
			func(n *Challenge, e *ChallengeInstance) {
				n.Edges.ChallengeInstances = append(n.Edges.ChallengeInstances, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withScoreRecords; query != nil {
		if err := _q.loadScoreRecords(ctx, query, nodes,
			func(n *Challenge) { n.Edges.ScoreRecords = []*ScoreRecord{} },
			func(n *Challenge, e *ScoreRecord) { n.Edges.ScoreRecords = append(n.Edges.ScoreRecords, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCheckResults; query != nil {
		if err := _q.loadCheckResults(ctx, query, nodes,
			func(n *Challenge) { n.Edges.CheckResults = []*CheckResult{} },
			func(n *Challenge, e *CheckResult) { n.Edges.CheckResults = append(n.Edges.CheckResults, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withWriteups; query != nil {
		if err := _q.loadWriteups(ctx, query, nodes,
			func(n *Challenge) { n.Edges.Writeups = []*Writeup{} },
			func(n *Challenge, e *Writeup) { n.Edges.Writeups = append(n.Edges.Writeups, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ChallengeQuery) loadAuthor(ctx context.Context, query *UserQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Challenge)
	for i := range nodes {
		fk := nodes[i].AuthorID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "author_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ChallengeQuery) loadHints(ctx context.Context, query *ChallengeHintQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *ChallengeHint)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(challengehint.FieldChallengeID)
	}
	query.Where(predicate.ChallengeHint(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.HintsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadTags(ctx context.Context, query *ChallengeTagQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *ChallengeTag)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ChallengeTag(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.TagsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.challenge_tags
		if fk == nil {
			return fmt.Errorf(`foreign-key "challenge_tags" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_tags" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadCompetitionChallenges(ctx context.Context, query *CompetitionChallengeQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *CompetitionChallenge)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(competitionchallenge.FieldChallengeID)
	}
	query.Where(predicate.CompetitionChallenge(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.CompetitionChallengesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadFlagSubmissions(ctx context.Context, query *FlagSubmissionQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *FlagSubmission)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(flagsubmission.FieldChallengeID)
	}
	query.Where(predicate.FlagSubmission(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.FlagSubmissionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadChallengeInstances(ctx context.Context, query *ChallengeInstanceQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *ChallengeInstance)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(challengeinstance.FieldChallengeID)
	}
	query.Where(predicate.ChallengeInstance(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.ChallengeInstancesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadScoreRecords(ctx context.Context, query *ScoreRecordQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *ScoreRecord)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(scorerecord.FieldChallengeID)
	}
	query.Where(predicate.ScoreRecord(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.ScoreRecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		if fk == nil {
			return fmt.Errorf(`foreign-key "challenge_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadCheckResults(ctx context.Context, query *CheckResultQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *CheckResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(checkresult.FieldChallengeID)
	}
	query.Where(predicate.CheckResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.CheckResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ChallengeQuery) loadWriteups(ctx context.Context, query *WriteupQuery, nodes []*Challenge, init func(*Challenge), assign func(*Challenge, *Writeup)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Challenge)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(writeup.FieldChallengeID)
	}
	query.Where(predicate.Writeup(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(challenge.WriteupsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChallengeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "challenge_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ChallengeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ChallengeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(challenge.Table, challenge.Columns, sqlgraph.NewFieldSpec(challenge.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, challenge.FieldID)
		for i := range fields {
			if fields[i] != challenge.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAuthor != nil {
			_spec.Node.AddColumnOnce(challenge.FieldAuthorID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ChallengeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(challenge.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = challenge.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ChallengeGroupBy is the group-by builder for Challenge entities.
type ChallengeGroupBy struct {
	selector
	build *ChallengeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ChallengeGroupBy) Aggregate(fns ...AggregateFunc) *ChallengeGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ChallengeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChallengeQuery, *ChallengeGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ChallengeGroupBy) sqlScan(ctx context.Context, root *ChallengeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ChallengeSelect is the builder for selecting fields of Challenge entities.
type ChallengeSelect struct {
	*ChallengeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ChallengeSelect) Aggregate(fns ...AggregateFunc) *ChallengeSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ChallengeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChallengeQuery, *ChallengeSelect](ctx, _s.ChallengeQuery, _s, _s.inters, v)
}

func (_s *ChallengeSelect) sqlScan(ctx context.Context, root *ChallengeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
