// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ZacharyZcR/STC/backend/ent/attackreport"
	"github.com/ZacharyZcR/STC/backend/ent/challengeinstance"
	"github.com/ZacharyZcR/STC/backend/ent/competition"
	"github.com/ZacharyZcR/STC/backend/ent/competitionchallenge"
	"github.com/ZacharyZcR/STC/backend/ent/defensereport"
	"github.com/ZacharyZcR/STC/backend/ent/exercisephase"
	"github.com/ZacharyZcR/STC/backend/ent/flagsubmission"
	"github.com/ZacharyZcR/STC/backend/ent/objective"
	"github.com/ZacharyZcR/STC/backend/ent/predicate"
	"github.com/ZacharyZcR/STC/backend/ent/scorerecord"
	"github.com/ZacharyZcR/STC/backend/ent/scoringround"
	"github.com/ZacharyZcR/STC/backend/ent/teamregistration"
	"github.com/ZacharyZcR/STC/backend/ent/user"
	"github.com/ZacharyZcR/STC/backend/ent/writeup"
)

// CompetitionQuery is the builder for querying Competition entities.
type CompetitionQuery struct {
	config
	ctx                       *QueryContext
	order                     []competition.OrderOption
	inters                    []Interceptor
	predicates                []predicate.Competition
	withCreator               *UserQuery
	withCompetitionChallenges *CompetitionChallengeQuery
	withRegistrations         *TeamRegistrationQuery
	withRounds                *ScoringRoundQuery
	withFlagSubmissions       *FlagSubmissionQuery
	withScoreRecords          *ScoreRecordQuery
	withChallengeInstances    *ChallengeInstanceQuery
	withWriteups              *WriteupQuery
	withObjectives            *ObjectiveQuery
	withAttackReports         *AttackReportQuery
	withDefenseReports        *DefenseReportQuery
	withPhases                *ExercisePhaseQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CompetitionQuery builder.
func (_q *CompetitionQuery) Where(ps ...predicate.Competition) *CompetitionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CompetitionQuery) Limit(limit int) *CompetitionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CompetitionQuery) Offset(offset int) *CompetitionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CompetitionQuery) Unique(unique bool) *CompetitionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CompetitionQuery) Order(o ...competition.OrderOption) *CompetitionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCreator chains the current query on the "creator" edge.
func (_q *CompetitionQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, competition.CreatorTable, competition.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCompetitionChallenges chains the current query on the "competition_challenges" edge.
func (_q *CompetitionQuery) QueryCompetitionChallenges() *CompetitionChallengeQuery {
	query := (&CompetitionChallengeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(competitionchallenge.Table, competitionchallenge.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.CompetitionChallengesTable, competition.CompetitionChallengesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRegistrations chains the current query on the "registrations" edge.
func (_q *CompetitionQuery) QueryRegistrations() *TeamRegistrationQuery {
	query := (&TeamRegistrationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(teamregistration.Table, teamregistration.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.RegistrationsTable, competition.RegistrationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRounds chains the current query on the "rounds" edge.
func (_q *CompetitionQuery) QueryRounds() *ScoringRoundQuery {
	query := (&ScoringRoundClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(scoringround.Table, scoringround.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.RoundsTable, competition.RoundsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFlagSubmissions chains the current query on the "flag_submissions" edge.
func (_q *CompetitionQuery) QueryFlagSubmissions() *FlagSubmissionQuery {
	query := (&FlagSubmissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(flagsubmission.Table, flagsubmission.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.FlagSubmissionsTable, competition.FlagSubmissionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryScoreRecords chains the current query on the "score_records" edge.
func (_q *CompetitionQuery) QueryScoreRecords() *ScoreRecordQuery {
	query := (&ScoreRecordClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(scorerecord.Table, scorerecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.ScoreRecordsTable, competition.ScoreRecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChallengeInstances chains the current query on the "challenge_instances" edge.
func (_q *CompetitionQuery) QueryChallengeInstances() *ChallengeInstanceQuery {
	query := (&ChallengeInstanceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(challengeinstance.Table, challengeinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.ChallengeInstancesTable, competition.ChallengeInstancesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWriteups chains the current query on the "writeups" edge.
func (_q *CompetitionQuery) QueryWriteups() *WriteupQuery {
	query := (&WriteupClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(writeup.Table, writeup.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.WriteupsTable, competition.WriteupsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryObjectives chains the current query on the "objectives" edge.
func (_q *CompetitionQuery) QueryObjectives() *ObjectiveQuery {
	query := (&ObjectiveClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(objective.Table, objective.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.ObjectivesTable, competition.ObjectivesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttackReports chains the current query on the "attack_reports" edge.
func (_q *CompetitionQuery) QueryAttackReports() *AttackReportQuery {
	query := (&AttackReportClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(attackreport.Table, attackreport.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.AttackReportsTable, competition.AttackReportsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDefenseReports chains the current query on the "defense_reports" edge.
func (_q *CompetitionQuery) QueryDefenseReports() *DefenseReportQuery {
	query := (&DefenseReportClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(defensereport.Table, defensereport.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.DefenseReportsTable, competition.DefenseReportsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPhases chains the current query on the "phases" edge.
func (_q *CompetitionQuery) QueryPhases() *ExercisePhaseQuery {
	query := (&ExercisePhaseClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(competition.Table, competition.FieldID, selector),
			sqlgraph.To(exercisephase.Table, exercisephase.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, competition.PhasesTable, competition.PhasesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Competition entity from the query.
// Returns a *NotFoundError when no Competition was found.
func (_q *CompetitionQuery) First(ctx context.Context) (*Competition, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{competition.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CompetitionQuery) FirstX(ctx context.Context) *Competition {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Competition ID from the query.
// Returns a *NotFoundError when no Competition ID was found.
func (_q *CompetitionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{competition.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CompetitionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Competition entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Competition entity is found.
// Returns a *NotFoundError when no Competition entities are found.
func (_q *CompetitionQuery) Only(ctx context.Context) (*Competition, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{competition.Label}
	default:
		return nil, &NotSingularError{competition.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CompetitionQuery) OnlyX(ctx context.Context) *Competition {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Competition ID in the query.
// Returns a *NotSingularError when more than one Competition ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CompetitionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{competition.Label}
	default:
		err = &NotSingularError{competition.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CompetitionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Competitions.
func (_q *CompetitionQuery) All(ctx context.Context) ([]*Competition, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Competition, *CompetitionQuery]()
	return withInterceptors[[]*Competition](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CompetitionQuery) AllX(ctx context.Context) []*Competition {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Competition IDs.
func (_q *CompetitionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(competition.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CompetitionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CompetitionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CompetitionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CompetitionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CompetitionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CompetitionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CompetitionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CompetitionQuery) Clone() *CompetitionQuery {
	if _q == nil {
		return nil
	}
	return &CompetitionQuery{
		config:                    _q.config,
		ctx:                       _q.ctx.Clone(),
		order:                     append([]competition.OrderOption{}, _q.order...),
		inters:                    append([]Interceptor{}, _q.inters...),
		predicates:                append([]predicate.Competition{}, _q.predicates...),
		withCreator:               _q.withCreator.Clone(),
		withCompetitionChallenges: _q.withCompetitionChallenges.Clone(),
		withRegistrations:         _q.withRegistrations.Clone(),
		withRounds:                _q.withRounds.Clone(),
		withFlagSubmissions:       _q.withFlagSubmissions.Clone(),
		withScoreRecords:          _q.withScoreRecords.Clone(),
		withChallengeInstances:    _q.withChallengeInstances.Clone(),
		withWriteups:              _q.withWriteups.Clone(),
		withObjectives:            _q.withObjectives.Clone(),
		withAttackReports:         _q.withAttackReports.Clone(),
		withDefenseReports:        _q.withDefenseReports.Clone(),
		withPhases:                _q.withPhases.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithCreator(opts ...func(*UserQuery)) *CompetitionQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreator = query
	return _q
}

// WithCompetitionChallenges tells the query-builder to eager-load the nodes that are connected to
// the "competition_challenges" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithCompetitionChallenges(opts ...func(*CompetitionChallengeQuery)) *CompetitionQuery {
	query := (&CompetitionChallengeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCompetitionChallenges = query
	return _q
}

// WithRegistrations tells the query-builder to eager-load the nodes that are connected to
// the "registrations" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithRegistrations(opts ...func(*TeamRegistrationQuery)) *CompetitionQuery {
	query := (&TeamRegistrationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRegistrations = query
	return _q
}

// WithRounds tells the query-builder to eager-load the nodes that are connected to
// the "rounds" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithRounds(opts ...func(*ScoringRoundQuery)) *CompetitionQuery {
	query := (&ScoringRoundClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRounds = query
	return _q
}

// WithFlagSubmissions tells the query-builder to eager-load the nodes that are connected to
// the "flag_submissions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithFlagSubmissions(opts ...func(*FlagSubmissionQuery)) *CompetitionQuery {
	query := (&FlagSubmissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFlagSubmissions = query
	return _q
}

// WithScoreRecords tells the query-builder to eager-load the nodes that are connected to
// the "score_records" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithScoreRecords(opts ...func(*ScoreRecordQuery)) *CompetitionQuery {
	query := (&ScoreRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withScoreRecords = query
	return _q
}

// WithChallengeInstances tells the query-builder to eager-load the nodes that are connected to
// the "challenge_instances" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithChallengeInstances(opts ...func(*ChallengeInstanceQuery)) *CompetitionQuery {
	query := (&ChallengeInstanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChallengeInstances = query
	return _q
}

// WithWriteups tells the query-builder to eager-load the nodes that are connected to
// the "writeups" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithWriteups(opts ...func(*WriteupQuery)) *CompetitionQuery {
	query := (&WriteupClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWriteups = query
	return _q
}

// WithObjectives tells the query-builder to eager-load the nodes that are connected to
// the "objectives" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithObjectives(opts ...func(*ObjectiveQuery)) *CompetitionQuery {
	query := (&ObjectiveClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withObjectives = query
	return _q
}

// WithAttackReports tells the query-builder to eager-load the nodes that are connected to
// the "attack_reports" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithAttackReports(opts ...func(*AttackReportQuery)) *CompetitionQuery {
	query := (&AttackReportClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAttackReports = query
	return _q
}

// WithDefenseReports tells the query-builder to eager-load the nodes that are connected to
// the "defense_reports" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithDefenseReports(opts ...func(*DefenseReportQuery)) *CompetitionQuery {
	query := (&DefenseReportClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDefenseReports = query
	return _q
}

// WithPhases tells the query-builder to eager-load the nodes that are connected to
// the "phases" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CompetitionQuery) WithPhases(opts ...func(*ExercisePhaseQuery)) *CompetitionQuery {
	query := (&ExercisePhaseClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPhases = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Competition.Query().
//		GroupBy(competition.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CompetitionQuery) GroupBy(field string, fields ...string) *CompetitionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CompetitionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = competition.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.Competition.Query().
//		Select(competition.FieldTitle).
//		Scan(ctx, &v)
func (_q *CompetitionQuery) Select(fields ...string) *CompetitionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CompetitionSelect{CompetitionQuery: _q}
	sbuild.label = competition.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CompetitionSelect configured with the given aggregations.
func (_q *CompetitionQuery) Aggregate(fns ...AggregateFunc) *CompetitionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CompetitionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !competition.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CompetitionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Competition, error) {
	var (
		nodes       = []*Competition{}
		_spec       = _q.querySpec()
		loadedTypes = [12]bool{
			_q.withCreator != nil,
			_q.withCompetitionChallenges != nil,
			_q.withRegistrations != nil,
			_q.withRounds != nil,
			_q.withFlagSubmissions != nil,
			_q.withScoreRecords != nil,
			_q.withChallengeInstances != nil,
			_q.withWriteups != nil,
			_q.withObjectives != nil,
			_q.withAttackReports != nil,
			_q.withDefenseReports != nil,
			_q.withPhases != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Competition).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Competition{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCreator; query != nil {
		if err := _q.loadCreator(ctx, query, nodes, nil,
			func(n *Competition, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCompetitionChallenges; query != nil {
		if err := _q.loadCompetitionChallenges(ctx, query, nodes,
			func(n *Competition) { n.Edges.CompetitionChallenges = []*CompetitionChallenge{} },
			func(n *Competition, e *CompetitionChallenge) {
				n.Edges.CompetitionChallenges = append(n.Edges.CompetitionChallenges, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withRegistrations; query != nil {
		if err := _q.loadRegistrations(ctx, query, nodes,
			func(n *Competition) { n.Edges.Registrations = []*TeamRegistration{} },
			func(n *Competition, e *TeamRegistration) { n.Edges.Registrations = append(n.Edges.Registrations, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRounds; query != nil {
		if err := _q.loadRounds(ctx, query, nodes,
			func(n *Competition) { n.Edges.Rounds = []*ScoringRound{} },
			func(n *Competition, e *ScoringRound) { n.Edges.Rounds = append(n.Edges.Rounds, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFlagSubmissions; query != nil {
		if err := _q.loadFlagSubmissions(ctx, query, nodes,
			func(n *Competition) { n.Edges.FlagSubmissions = []*FlagSubmission{} },
			func(n *Competition, e *FlagSubmission) { n.Edges.FlagSubmissions = append(n.Edges.FlagSubmissions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withScoreRecords; query != nil {
		if err := _q.loadScoreRecords(ctx, query, nodes,
			func(n *Competition) { n.Edges.ScoreRecords = []*ScoreRecord{} },
			func(n *Competition, e *ScoreRecord) { n.Edges.ScoreRecords = append(n.Edges.ScoreRecords, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChallengeInstances; query != nil {
		if err := _q.loadChallengeInstances(ctx, query, nodes,
			func(n *Competition) { n.Edges.ChallengeInstances = []*ChallengeInstance{} },
			func(n *Competition, e *ChallengeInstance) {
				n.Edges.ChallengeInstances = append(n.Edges.ChallengeInstances, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withWriteups; query != nil {
		if err := _q.loadWriteups(ctx, query, nodes,
			func(n *Competition) { n.Edges.Writeups = []*Writeup{} },
			func(n *Competition, e *Writeup) { n.Edges.Writeups = append(n.Edges.Writeups, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withObjectives; query != nil {
		if err := _q.loadObjectives(ctx, query, nodes,
			func(n *Competition) { n.Edges.Objectives = []*Objective{} },
			func(n *Competition, e *Objective) { n.Edges.Objectives = append(n.Edges.Objectives, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAttackReports; query != nil {
		if err := _q.loadAttackReports(ctx, query, nodes,
			func(n *Competition) { n.Edges.AttackReports = []*AttackReport{} },
			func(n *Competition, e *AttackReport) { n.Edges.AttackReports = append(n.Edges.AttackReports, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withDefenseReports; query != nil {
		if err := _q.loadDefenseReports(ctx, query, nodes,
			func(n *Competition) { n.Edges.DefenseReports = []*DefenseReport{} },
			func(n *Competition, e *DefenseReport) { n.Edges.DefenseReports = append(n.Edges.DefenseReports, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPhases; query != nil {
		if err := _q.loadPhases(ctx, query, nodes,
			func(n *Competition) { n.Edges.Phases = []*ExercisePhase{} },
			func(n *Competition, e *ExercisePhase) { n.Edges.Phases = append(n.Edges.Phases, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CompetitionQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Competition)
	for i := range nodes {
		fk := nodes[i].CreatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CompetitionQuery) loadCompetitionChallenges(ctx context.Context, query *CompetitionChallengeQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *CompetitionChallenge)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(competitionchallenge.FieldCompetitionID)
	}
	query.Where(predicate.CompetitionChallenge(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.CompetitionChallengesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadRegistrations(ctx context.Context, query *TeamRegistrationQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *TeamRegistration)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamregistration.FieldCompetitionID)
	}
	query.Where(predicate.TeamRegistration(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.RegistrationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadRounds(ctx context.Context, query *ScoringRoundQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *ScoringRound)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(scoringround.FieldCompetitionID)
	}
	query.Where(predicate.ScoringRound(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.RoundsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadFlagSubmissions(ctx context.Context, query *FlagSubmissionQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *FlagSubmission)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(flagsubmission.FieldCompetitionID)
	}
	query.Where(predicate.FlagSubmission(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.FlagSubmissionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadScoreRecords(ctx context.Context, query *ScoreRecordQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *ScoreRecord)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(scorerecord.FieldCompetitionID)
	}
	query.Where(predicate.ScoreRecord(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.ScoreRecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadChallengeInstances(ctx context.Context, query *ChallengeInstanceQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *ChallengeInstance)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(challengeinstance.FieldCompetitionID)
	}
	query.Where(predicate.ChallengeInstance(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.ChallengeInstancesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadWriteups(ctx context.Context, query *WriteupQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *Writeup)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(writeup.FieldCompetitionID)
	}
	query.Where(predicate.Writeup(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.WriteupsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadObjectives(ctx context.Context, query *ObjectiveQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *Objective)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(objective.FieldCompetitionID)
	}
	query.Where(predicate.Objective(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.ObjectivesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadAttackReports(ctx context.Context, query *AttackReportQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *AttackReport)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(attackreport.FieldCompetitionID)
	}
	query.Where(predicate.AttackReport(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.AttackReportsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadDefenseReports(ctx context.Context, query *DefenseReportQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *DefenseReport)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(defensereport.FieldCompetitionID)
	}
	query.Where(predicate.DefenseReport(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.DefenseReportsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CompetitionQuery) loadPhases(ctx context.Context, query *ExercisePhaseQuery, nodes []*Competition, init func(*Competition), assign func(*Competition, *ExercisePhase)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Competition)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(exercisephase.FieldCompetitionID)
	}
	query.Where(predicate.ExercisePhase(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(competition.PhasesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CompetitionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "competition_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CompetitionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CompetitionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(competition.Table, competition.Columns, sqlgraph.NewFieldSpec(competition.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, competition.FieldID)
		for i := range fields {
			if fields[i] != competition.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCreator != nil {
			_spec.Node.AddColumnOnce(competition.FieldCreatedBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CompetitionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(competition.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = competition.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CompetitionGroupBy is the group-by builder for Competition entities.
type CompetitionGroupBy struct {
	selector
	build *CompetitionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CompetitionGroupBy) Aggregate(fns ...AggregateFunc) *CompetitionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CompetitionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CompetitionQuery, *CompetitionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CompetitionGroupBy) sqlScan(ctx context.Context, root *CompetitionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CompetitionSelect is the builder for selecting fields of Competition entities.
type CompetitionSelect struct {
	*CompetitionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CompetitionSelect) Aggregate(fns ...AggregateFunc) *CompetitionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CompetitionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CompetitionQuery, *CompetitionSelect](ctx, _s.CompetitionQuery, _s, _s.inters, v)
}

func (_s *CompetitionSelect) sqlScan(ctx context.Context, root *CompetitionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
